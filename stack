#include<bits/stdc++.h>
using namespace std;
int main()
{
  stack<string>s;
  s.push("harsh");
  s.push("vivek");
  s.push("nilesh");
  s.push("ram");
  cout<<s.top()<<endl;
// for printting all the elements while print element and pop it and then another one come then pop it also but first print it 
//   while(! s.empty()){
//     cout<<s.top()<<endl;
//     s.pop();
//   }
  cout<<endl;
  s.pop();
  cout<<"top element is "<<s.top();
  return 0; 
}
